import { BookResolvers } from "src/types";

// for custom logic for any field on the type, in this case Book
// by default query will attempt to provide the typed field responses but can use this if we need some custom logic

// parent = the book object
export const bookResolvers: BookResolvers = {};
